import { BgcolorPokemonDirective } from './bgcolor-pokemon.directive';

describe('BgcolorPokemonDirective', () => {
  it('should create an instance', () => {
    const directive = new BgcolorPokemonDirective();
    expect(directive).toBeTruthy();
  });
});

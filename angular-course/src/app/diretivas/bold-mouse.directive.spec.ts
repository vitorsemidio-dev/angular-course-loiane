import { BoldMouseDirective } from './bold-mouse.directive';

describe('BoldMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldMouseDirective();
    expect(directive).toBeTruthy();
  });
});
